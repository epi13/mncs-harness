# Hostile end-to-end evidence (second conversion campaign)

Generated by `scripts/hostile_mncs_e2e.py` (re-runnable). Every scenario
drives a production function that EXECUTES MNCS through `mncs_exec` — no
mirror, no mocks — with adversarial input:

- risky tool requests (sudo, shell-string, `python -c`, forced push) all
  refuse with distinct reasons; the benign control allows;
- conflicting capability evidence resolves by recency, garbage outcomes
  fail closed under a fail-closed policy;
- readiness holds UNKNOWN, orders BLOCKED first, degrades honestly;
- Atlas folds deny over grants and never promote UNKNOWN, preserving the
  outstanding-evidence union;
- placement without fallback closes (`PIN_FAILED_CLOSED`); ungranted
  tools refuse;
- empty resource facts refuse with the facts-required message;
- a single flipped byte in a shipped artifact refuses to load
  (`MncsRuntimeError`);
- the canonical path emits no transitional warnings.

`report.json`: 19 scenarios, 19 pass (2026-09-06).

# Expected diagnostics for HARNESS-PRESSURE-002

Sealed against mncs-language `source-study` output (2026-09-06).
Re-run: `cargo run -p mncs-cli -- source-study repro.mncs --node-id <id>`
from an mncs-language checkout.

- `MNE105 :: source profile supports bool, byte, 8/16/32/64-bit integers, declared finite/record types, bounded sequences, and Profile 0.8 integer vec<T, N>/mask<N>`
